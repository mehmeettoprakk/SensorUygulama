import { Card, PanelContent } from "@/components";
import React from "react";

const Sensorler = () => {
  return (
    <PanelContent title="Profili Düzenle">
      <Card title="Profil"></Card>
    </PanelContent>
  );
};

export default Sensorler;
